import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  root: {
    flexDirection: "row",
    justifyContent: "space-evenly",
    alignItems: "center",
    height: "100%",
  },
});
